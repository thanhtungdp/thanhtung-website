---
import { Image } from 'astro:assets';
import BaseHead from '../BaseHead.astro';
import Footer from '../Footer.astro';
import Header from '../Header.astro';
import { SITE_TITLE } from '../../consts';
import { localizePath, type Locale } from '../../i18n';
import EventFeature from '../../assets/projects/sale-ai/event-demo-feature.webp';
import AgentConceptSlide from '../../assets/projects/sale-ai/slide-03.webp';
import CaseSlide from '../../assets/projects/sale-ai/slide-04.webp';
import EnterpriseImpactSlide from '../../assets/projects/sale-ai/slide-06.webp';
import PainSlide from '../../assets/projects/sale-ai/slide-08.webp';
import StrategySlide from '../../assets/projects/sale-ai/slide-09.webp';
import QuoteSlide from '../../assets/projects/sale-ai/slide-10.webp';
import FlowSlide from '../../assets/projects/sale-ai/slide-14.webp';
import StopwatchSlide from '../../assets/projects/sale-ai/slide-15.webp';
import OperatingModelSlide from '../../assets/projects/sale-ai/slide-17.webp';
import KpiSlide from '../../assets/projects/sale-ai/slide-20.webp';

interface Props {
	locale: Locale;
}

const { locale } = Astro.props;
const isVi = locale === 'vi';

const copy = isVi
	? {
			title: 'ADG Sale AI - AI Agent cho hệ thống phân phối quy mô lớn',
			description:
				'Project detail về ADG Sale AI: lớp AI Agent cho hệ thống phân phối 2k đại lý, giúp tư vấn, báo giá, tạo đơn hàng và tra cứu kinh doanh dưới 60 giây.',
			back: '← Về trang chủ',
			badge: 'Sale AI case study',
			role: 'AI Agent / Product Architect',
			heroTitle: 'AI Agent bán hàng cho mạng lưới 2k đại lý.',
			heroLead:
				'ADG Sale AI tối ưu lớp giao tiếp cuối giữa đại lý, sales và hệ thống lõi SAP/CRM: tư vấn từ hình ảnh, so sánh sản phẩm, tạo báo giá, tạo đơn hàng và tra cứu dữ liệu kinh doanh 24/7.',
			cta: 'Xem mô hình Sale AI',
			statsTitle: 'Impact từ case study',
			stats: [
				{ value: '2k', label: 'đại lý C1/C2 trong hệ thống phân phối' },
				{ value: '<60s', label: 'mục tiêu tạo báo giá và đơn hàng' },
				{ value: '5-15m', label: 'thời gian phản hồi cũ tại điểm bán' },
				{ value: '70-80%', label: 'giảm thời gian tư vấn & báo giá' },
				{ value: '10-15%', label: 'doanh thu tăng thêm minh họa' },
				{ value: '6-9m', label: 'payback period ước tính' },
			],
			modelTitle: 'Lớp AI Agent nằm giữa con người và hệ thống lõi',
			modelLead:
				'Giải pháp không thay thế SAP/CRM. Nó đóng vai trò lớp giao tiếp tự nhiên giúp đại lý và đội sales khai thác dữ liệu, tài liệu kỹ thuật, bảng giá và quy trình đặt hàng dễ hơn ngay tại điểm bán.',
			layers: [
				{ title: 'Tư vấn từ hình ảnh', text: 'Nhận ảnh/ngữ cảnh khách hàng để gợi ý dòng cửa, kích thước, vật liệu và cấu hình phù hợp.' },
				{ title: 'So sánh sản phẩm', text: 'Đối chiếu theo ngân sách, kích thước, nhu cầu, quy cách kỹ thuật và điều kiện thi công.' },
				{ title: 'Báo giá dưới 60 giây', text: 'Tạo báo giá nhanh, chuẩn dữ liệu và giảm sai sót so với thao tác thủ công nhiều bước.' },
				{ title: 'Tạo đơn hàng dưới 60 giây', text: 'Chuyển tư vấn thành đơn hàng với dữ liệu đồng bộ về hệ thống nghiệp vụ lõi.' },
				{ title: 'Business lookup 24/7', text: 'Tra cứu doanh số, đơn hàng, tài liệu kỹ thuật và chính sách theo quyền của từng đại lý.' },
				{ title: 'Human + AI governance', text: 'AI xử lý việc lặp lại, con người giám sát tư vấn, bảo mật và quyết định cuối cùng.' },
			],
			implementationTitle: 'Từ pain point bán hàng tới flow chốt đơn 2 phút',
			implementationLead:
				'Deck mô tả bài toán ngành cửa: sản phẩm đa dạng, bảng giá khó nhớ, tư vấn dễ sai, khách hàng kỳ vọng phản hồi tức thì. Sale AI gom quy trình thành flow: upload ảnh, AI gợi ý, báo giá 60 giây và lên đơn.',
			proofTitle: 'Board-level KPI & ROI',
			cases: [
				{ value: '+5%', label: 'conversion uplift', text: 'Kịch bản minh họa tăng chuyển đổi từ 20% lên 25% nhờ phản hồi nhanh và tư vấn chuẩn.' },
				{ value: '3-5x', label: 'ROI năm đầu', text: 'Ước tính tác động tài chính so với chi phí đầu tư AI Agent trong năm đầu.' },
				{ value: '10 khách', label: 'mỗi đại lý / ngày', text: 'Giả định quy mô vận hành để lượng hóa tác động cho mạng lưới 2k đại lý.' },
				{ value: '24/7', label: 'hỗ trợ đại lý', text: 'Tra cứu dữ liệu kinh doanh, kỹ thuật và đào tạo liên tục, cá nhân hóa theo đại lý.' },
			],
			featureTitle: 'Core capabilities',
			features: ['Image-to-quote', 'Product comparison', 'Quote <60s', 'Order <60s', 'Dealer personalization', 'SAP / CRM bridge', 'Technical documents', 'Training assistant', 'AI adoption KPI', 'Human-in-the-loop'],
			galleryTitle: 'Sale AI được diễn dịch thành hệ thống',
			visualBlocks: [
				{ label: 'Input', title: 'Ảnh & nhu cầu khách hàng', text: 'Đại lý chụp ảnh công trình, nhập ngân sách, kích thước và ngữ cảnh bán hàng.' },
				{ label: 'Agent', title: 'Sale AI tư vấn', text: 'AI đọc tài liệu kỹ thuật, bảng giá, chính sách và lịch sử bán hàng để đề xuất phương án.' },
				{ label: 'System', title: 'SAP / CRM đồng bộ', text: 'Dữ liệu báo giá, đơn hàng và quyền đại lý được kiểm soát qua hệ thống lõi.' },
				{ label: 'Output', title: 'Báo giá, đơn hàng, KPI', text: 'Sales và lãnh đạo thấy tốc độ phản hồi, conversion, doanh thu và adoption theo thời gian thực.' },
			],
			workflow: ['Upload ảnh', 'AI gợi ý cấu hình', 'Báo giá <60s', 'Tạo đơn hàng', 'Dashboard KPI'],
			governance: [
				{ title: 'Human-in-the-loop', text: 'Con người duyệt khuyến nghị quan trọng, AI xử lý phần lặp lại và tra cứu nhanh.' },
				{ title: 'Dealer permission', text: 'Mỗi đại lý chỉ thấy dữ liệu, chính sách và bảng giá theo quyền được cấp.' },
				{ title: 'Adoption rhythm', text: 'Theo dõi tỷ lệ dùng AI, thời gian phản hồi và hiệu quả chuyển đổi theo tuần.' },
			],
			closing: 'Sale AI là ví dụ sắc nét cho cách tôi triển khai AI trong doanh nghiệp lớn: không thay lõi ERP/CRM, mà tối ưu điểm chạm cuối nơi tốc độ, trải nghiệm và doanh thu được quyết định.',
			contact: 'Trao đổi về Sale AI / AI Agent cho sales',
			gallery: [
				{ title: 'AI Agent trong chuỗi giá trị', image: AgentConceptSlide },
				{ title: 'Case ADG Sale AI tại hội nghị đại lý', image: CaseSlide },
				{ title: 'Bức tranh hiệu quả AI toàn doanh nghiệp', image: EnterpriseImpactSlide },
				{ title: 'Nỗi đau ngành cửa', image: PainSlide },
				{ title: 'Mục tiêu chiến lược', image: StrategySlide },
				{ title: 'Báo giá 60 giây', image: QuoteSlide },
				{ title: 'Flow 120 giây chốt đơn', image: FlowSlide },
				{ title: 'Nguyên tắc Human + AI', image: OperatingModelSlide },
				{ title: 'KPI chính của dự án', image: KpiSlide },
			],
		}
	: {
			title: 'ADG Sale AI - AI Agent for large-scale distribution networks',
			description:
				'ADG Sale AI project detail: an AI Agent layer for a 2k-dealer distribution system, enabling consultation, quoting, order creation, and business lookup in under 60 seconds.',
			back: '← Back home',
			badge: 'Sale AI case study',
			role: 'AI Agent / Product Architect',
			heroTitle: 'A sales AI Agent for a 2k-dealer network.',
			heroLead:
				'ADG Sale AI optimizes the last-mile interface between dealers, sales teams, and SAP/CRM: image-based consultation, product comparison, quotes, orders, and 24/7 business lookup.',
			cta: 'View Sale AI model',
			statsTitle: 'Case-study impact',
			stats: [
				{ value: '2k', label: 'C1/C2 dealers in the distribution network' },
				{ value: '<60s', label: 'target quote and order creation time' },
				{ value: '5-15m', label: 'previous point-of-sale response time' },
				{ value: '70-80%', label: 'less consultation and quoting time' },
				{ value: '10-15%', label: 'illustrative incremental revenue' },
				{ value: '6-9m', label: 'estimated payback period' },
			],
			modelTitle: 'An AI Agent layer between people and core systems',
			modelLead:
				'The solution does not replace SAP/CRM. It acts as a natural-language interface that helps dealers and sales teams use data, technical documents, pricing, and ordering workflows at the point of sale.',
			layers: [
				{ title: 'Image-based consultation', text: 'Use customer images and context to recommend door lines, dimensions, materials, and configurations.' },
				{ title: 'Product comparison', text: 'Compare options by budget, dimensions, needs, technical specifications, and installation context.' },
				{ title: 'Quotes under 60 seconds', text: 'Generate fast, data-aligned quotes while reducing errors from manual multi-step work.' },
				{ title: 'Orders under 60 seconds', text: 'Turn consultation into orders with synchronized data back to core business systems.' },
				{ title: 'Business lookup 24/7', text: 'Retrieve sales, orders, technical documents, and policies based on each dealer’s permissions.' },
				{ title: 'Human + AI governance', text: 'AI handles repetitive work while humans supervise recommendations, security, and final decisions.' },
			],
			implementationTitle: 'From sales pain points to a 2-minute closing flow',
			implementationLead:
				'The deck frames the door industry pain points: diverse products, hard-to-remember price rules, error-prone consultation, and customers expecting instant responses. Sale AI compresses the workflow into: upload image, AI suggestion, 60-second quote, and order creation.',
			proofTitle: 'Board-level KPI & ROI',
			cases: [
				{ value: '+5%', label: 'conversion uplift', text: 'Illustrative scenario: conversion improves from 20% to 25% through faster response and standardized consultation.' },
				{ value: '3-5x', label: 'first-year ROI', text: 'Estimated financial impact versus AI Agent investment cost in year one.' },
				{ value: '10 clients', label: 'per dealer / day', text: 'Operating assumption used to quantify impact across a 2k-dealer network.' },
				{ value: '24/7', label: 'dealer support', text: 'Continuous business, technical, and training lookup personalized by dealer.' },
			],
			featureTitle: 'Core capabilities',
			features: ['Image-to-quote', 'Product comparison', 'Quote <60s', 'Order <60s', 'Dealer personalization', 'SAP / CRM bridge', 'Technical documents', 'Training assistant', 'AI adoption KPI', 'Human-in-the-loop'],
			galleryTitle: 'Sale AI translated into operating blocks',
			visualBlocks: [
				{ label: 'Input', title: 'Customer image & intent', text: 'Dealers capture the site, budget, dimensions, and sales context at the point of sale.' },
				{ label: 'Agent', title: 'Sale AI consultation', text: 'The AI reads technical documents, pricing rules, policies, and sales history to recommend options.' },
				{ label: 'System', title: 'SAP / CRM sync', text: 'Quote, order, and dealer-permission data stay governed by the core business stack.' },
				{ label: 'Output', title: 'Quotes, orders, KPI', text: 'Sales teams and leaders track response speed, conversion, revenue, and adoption in real time.' },
			],
			workflow: ['Upload image', 'AI config suggestion', 'Quote <60s', 'Create order', 'KPI dashboard'],
			governance: [
				{ title: 'Human-in-the-loop', text: 'Humans approve important recommendations while AI handles repetitive lookup and drafting.' },
				{ title: 'Dealer permission', text: 'Each dealer only sees the data, policy, and pricing they are allowed to access.' },
				{ title: 'Adoption rhythm', text: 'Track AI usage, response time, and conversion performance week by week.' },
			],
			closing: 'Sale AI shows how I deploy AI in large enterprises: not by replacing ERP/CRM, but by optimizing the final customer-facing layer where speed, experience, and revenue are decided.',
			contact: 'Discuss Sale AI / sales AI Agents',
			gallery: [
				{ title: 'AI Agents in the value chain', image: AgentConceptSlide },
				{ title: 'ADG Sale AI at the dealer conference', image: CaseSlide },
				{ title: 'Enterprise-wide AI impact map', image: EnterpriseImpactSlide },
				{ title: 'Door-industry pain points', image: PainSlide },
				{ title: 'Strategic objective', image: StrategySlide },
				{ title: '60-second quote', image: QuoteSlide },
				{ title: '2-minute closing flow', image: FlowSlide },
				{ title: 'Human + AI principles', image: OperatingModelSlide },
				{ title: 'Project KPIs', image: KpiSlide },
			],
		};
---

<BaseHead title={`${copy.title} | ${SITE_TITLE}`} description={copy.description} />

<body class="sale-ai-project-shell">
	<Header locale={locale} />
	<main>
		<div class="page-wrap">
			<section class="hero-section">
				<div class="hero-copy">
					<a class="back-link" href={localizePath('/', locale)}>{copy.back}</a>
					<div class="eyebrow"><span>{copy.badge}</span><span>{copy.role}</span></div>
					<h1>{copy.heroTitle}</h1>
					<p>{copy.heroLead}</p>
					<a class="primary-link" href="#model">{copy.cta}</a>
				</div>
				<div class="hero-visual">
					<Image src={EventFeature} alt="ADG Sale AI dealer conference product demo" widths={[640, 900, 1200]} sizes="(max-width: 900px) 92vw, 45vw" loading="eager" />
				</div>
			</section>

			<section class="stats-section" aria-labelledby="stats-title">
				<div>
					<p class="section-kicker">Numbers</p>
					<h2 id="stats-title">{copy.statsTitle}</h2>
				</div>
				<div class="stats-grid">
					{copy.stats.map((stat) => (
						<article>
							<strong>{stat.value}</strong>
							<span>{stat.label}</span>
						</article>
					))}
				</div>
			</section>

			<section class="model-section" id="model">
				<div class="section-heading">
					<p class="section-kicker">AI Agent layer</p>
					<h2>{copy.modelTitle}</h2>
					<p>{copy.modelLead}</p>
				</div>
				<div class="layer-grid">
					{copy.layers.map((layer, index) => (
						<article>
							<span>{String(index + 1).padStart(2, '0')}</span>
							<h3>{layer.title}</h3>
							<p>{layer.text}</p>
						</article>
					))}
				</div>
			</section>

			<section class="feature-section">
				<div class="feature-copy">
					<p class="section-kicker">Sales workflow</p>
					<h2>{copy.implementationTitle}</h2>
					<p>{copy.implementationLead}</p>
					<div class="feature-cloud">
						{copy.features.map((feature) => <span>{feature}</span>)}
					</div>
				</div>
				<div class="sales-diagram" aria-label="120-second sales workflow diagram">
					<div class="diagram-phone">
						<span>Dealer app</span>
						<strong>Site photo</strong>
						<small>Budget / size / intent</small>
					</div>
					<div class="diagram-agent">
						<span>Sale AI</span>
						<strong>Product match</strong>
						<small>Policy + price + spec</small>
					</div>
					<div class="diagram-output">
						<span>&lt;60s</span>
						<strong>Quote + order</strong>
						<small>SAP / CRM synced</small>
					</div>
				</div>
			</section>

			<section class="proof-section">
				<div class="section-heading compact">
					<p class="section-kicker">Proof</p>
					<h2>{copy.proofTitle}</h2>
				</div>
				<div class="proof-grid">
					{copy.cases.map((item) => (
						<article>
							<strong>{item.value}</strong>
							<span>{item.label}</span>
							<p>{item.text}</p>
						</article>
					))}
				</div>
			</section>

			<section class="visual-story-section">
				<div class="section-heading compact">
					<h2>{copy.galleryTitle}</h2>
				</div>

				<div class="agent-map" aria-label="Sale AI operating map">
					<div class="agent-orbit">
						<span></span>
						<strong>Sale AI</strong>
						<small>Agent Layer</small>
					</div>
					{copy.visualBlocks.map((block) => (
						<article class="agent-card">
							<span>{block.label}</span>
							<h3>{block.title}</h3>
							<p>{block.text}</p>
						</article>
					))}
				</div>

				<div class="workflow-rail" aria-label="Sale AI workflow">
					{copy.workflow.map((step, index) => (
						<div class="workflow-step" style={`--step-index: ${index}`}>
							<span>{String(index + 1).padStart(2, '0')}</span>
							<strong>{step}</strong>
						</div>
					))}
				</div>

				<div class="governance-grid">
					{copy.governance.map((item) => (
						<article>
							<h3>{item.title}</h3>
							<p>{item.text}</p>
						</article>
					))}
				</div>
			</section>

			<section class="closing-section">
				<p>{copy.closing}</p>
				<a class="primary-link" href="mailto:hello@thanhtung.com">{copy.contact}</a>
			</section>
		</div>
	</main>
	<Footer />
</body>

<style>
	:global(body.sale-ai-project-shell) {
		margin: 0;
		background:
			radial-gradient(circle at top left, rgb(255 128 0 / 0.14), transparent 34rem),
			linear-gradient(180deg, #fffaf5 0%, #fff 36%, #f6f2ec 100%);
		color: #17120d;
		font-family: Arial, Helvetica, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
	}

	:global(.sale-ai-project-shell main) {
		min-height: 100vh;
	}

	.page-wrap {
		width: min(1180px, calc(100% - 2rem));
		margin: 0 auto;
	}

	.hero-section {
		display: grid;
		grid-template-columns: minmax(0, 0.92fr) minmax(340px, 1.08fr);
		gap: clamp(1.25rem, 4vw, 3.8rem);
		align-items: center;
		min-height: min(690px, calc(100dvh - 88px));
		padding: clamp(1rem, 3vw, 2.6rem) 0 clamp(2rem, 5vw, 4rem);
	}

	.back-link {
		color: #8a4f17;
		font-weight: 800;
		font-size: 0.92rem;
		text-decoration: none;
	}

	.hero-copy {
		display: grid;
		gap: 1.3rem;
	}

	.eyebrow,
	.feature-cloud {
		display: flex;
		flex-wrap: wrap;
		gap: 0.55rem;
	}

	.eyebrow span,
	.feature-cloud span {
		border: 1px solid rgb(236 115 21 / 0.24);
		background: rgb(255 255 255 / 0.82);
		color: #8f4e10;
		font-weight: 850;
		font-size: 0.78rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		padding: 0.52rem 0.78rem;
		border-radius: 999px;
	}

	h1 {
		font-size: clamp(2.65rem, 5.25vw, 5.45rem);
		letter-spacing: -0.066em;
		line-height: 0.96;
		max-width: 11.8ch;
		margin: 0;
	}

	.hero-copy p,
	.section-heading p,
	.feature-copy p,
	.closing-section p {
		font-size: clamp(1.02rem, 1.5vw, 1.22rem);
		line-height: 1.7;
		color: #5f5148;
		margin: 0;
	}

	.primary-link {
		display: inline-flex;
		width: fit-content;
		align-items: center;
		justify-content: center;
		border-radius: 999px;
		background: #f58220;
		color: #fff;
		font-weight: 900;
		padding: 0.92rem 1.18rem;
		text-decoration: none;
		box-shadow: 0 18px 45px rgb(245 130 32 / 0.24);
	}

	.hero-visual,
	.feature-section,
	.stats-section,
	.visual-story-section,
	.closing-section {
		border: 1px solid rgb(36 24 10 / 0.08);
		border-radius: 34px;
		background: rgb(255 255 255 / 0.76);
		box-shadow: 0 28px 70px rgb(48 31 9 / 0.1);
	}

	.hero-visual {
		padding: clamp(0.7rem, 1.8vw, 1.35rem);
		transform: rotate(1.1deg);
	}

	.hero-visual img {
		width: 100%;
		height: auto;
		border-radius: 24px;
		display: block;
	}

	.stats-section,
	.model-section,
	.feature-section,
	.proof-section,
	.visual-story-section,
	.closing-section {
		margin: clamp(2.5rem, 8vw, 6.4rem) 0;
	}

	.stats-section,
	.feature-section,
	.visual-story-section,
	.closing-section {
		padding: clamp(1.4rem, 4vw, 3rem);
	}

	.stats-section {
		display: grid;
		grid-template-columns: 0.72fr 1.28fr;
		gap: clamp(1.2rem, 4vw, 3rem);
		align-items: start;
	}

	.section-kicker {
		margin: 0 0 0.75rem;
		font-size: 0.78rem;
		font-weight: 900;
		letter-spacing: 0.14em;
		text-transform: uppercase;
		color: #f58220;
	}

	h2 {
		font-size: clamp(2rem, 4vw, 4.4rem);
		letter-spacing: -0.055em;
		line-height: 0.96;
		margin: 0;
	}

	.stats-grid,
	.layer-grid,
	.proof-grid {
		display: grid;
		gap: 1rem;
	}

	.stats-grid {
		grid-template-columns: repeat(3, minmax(0, 1fr));
	}

	.stats-grid article,
	.layer-grid article,
	.proof-grid article {
		border: 1px solid rgb(36 24 10 / 0.08);
		border-radius: 24px;
		background: rgb(255 255 255 / 0.86);
		padding: 1rem;
		margin: 0;
	}

	.stats-grid strong,
	.proof-grid strong {
		display: block;
		font-family: Arial, Helvetica, ui-sans-serif, system-ui, sans-serif;
		font-size: clamp(1.62rem, 3vw, 2.95rem);
		font-variant-numeric: tabular-nums;
		font-feature-settings: 'zero' 0;
		line-height: 1;
		letter-spacing: -0.025em;
		white-space: nowrap;
		color: #f58220;
	}

	.stats-grid span,
	.proof-grid span {
		display: block;
		margin-top: 0.6rem;
		color: #5f5148;
		font-weight: 750;
		line-height: 1.35;
	}

	.section-heading {
		max-width: 760px;
		margin-bottom: clamp(1.3rem, 4vw, 2.4rem);
	}

	.section-heading.compact {
		max-width: 680px;
	}

	.layer-grid {
		grid-template-columns: repeat(3, minmax(0, 1fr));
	}

	.layer-grid span {
		color: #f58220;
		font-weight: 950;
		font-size: 0.84rem;
	}

	.layer-grid h3 {
		font-size: 1.15rem;
		margin: 0.8rem 0 0.55rem;
	}

	.layer-grid p,
	.proof-grid p {
		color: #685a50;
		line-height: 1.55;
		margin: 0;
	}

	.feature-section {
		display: grid;
		grid-template-columns: minmax(0, 0.86fr) minmax(320px, 1.14fr);
		gap: clamp(1rem, 4vw, 3rem);
		align-items: center;
	}

	.feature-copy {
		display: grid;
		gap: 1.2rem;
	}

	.sales-diagram {
		position: relative;
		display: grid;
		grid-template-columns: 0.86fr 1fr;
		gap: 1rem;
		min-height: 430px;
		padding: clamp(1rem, 2vw, 1.35rem);
		border-radius: 28px;
		background:
			radial-gradient(circle at 70% 15%, rgb(245 130 32 / 0.18), transparent 18rem),
			linear-gradient(135deg, #17120d, #2b1f15);
		color: #fff;
		overflow: hidden;
	}

	.sales-diagram::before {
		content: '';
		position: absolute;
		inset: 18% 10%;
		border: 1px dashed rgb(255 255 255 / 0.25);
		border-radius: 999px;
		transform: rotate(-9deg);
	}

	.diagram-phone,
	.diagram-agent,
	.diagram-output {
		position: relative;
		z-index: 1;
		border: 1px solid rgb(255 255 255 / 0.13);
		background: rgb(255 255 255 / 0.08);
		box-shadow: inset 0 1px 0 rgb(255 255 255 / 0.14), 0 20px 55px rgb(0 0 0 / 0.18);
		backdrop-filter: blur(18px);
	}

	.diagram-phone {
		align-self: end;
		min-height: 260px;
		padding: 1.1rem;
		border-radius: 28px;
	}

	.diagram-agent {
		align-self: start;
		padding: 1.25rem;
		border-radius: 999px;
		text-align: center;
	}

	.diagram-output {
		grid-column: 1 / -1;
		justify-self: end;
		width: min(100%, 440px);
		padding: 1.1rem;
		border-radius: 24px;
	}

	.diagram-phone span,
	.diagram-agent span,
	.diagram-output span {
		display: inline-flex;
		margin-bottom: 0.8rem;
		border-radius: 999px;
		background: rgb(245 130 32 / 0.22);
		color: #ffd8b1;
		font-weight: 900;
		font-size: 0.74rem;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		padding: 0.38rem 0.62rem;
	}

	.diagram-phone strong,
	.diagram-agent strong,
	.diagram-output strong {
		display: block;
		font-size: clamp(1.25rem, 2vw, 1.8rem);
		letter-spacing: -0.04em;
	}

	.diagram-phone small,
	.diagram-agent small,
	.diagram-output small {
		display: block;
		margin-top: 0.45rem;
		color: rgb(255 255 255 / 0.62);
		font-weight: 750;
	}

	.proof-grid {
		grid-template-columns: repeat(4, minmax(0, 1fr));
	}

	.visual-story-section {
		background:
			linear-gradient(135deg, rgb(255 255 255 / 0.94), rgb(255 244 233 / 0.74)),
			radial-gradient(circle at 72% 22%, rgb(245 130 32 / 0.16), transparent 22rem);
		overflow: hidden;
	}

	.agent-map {
		position: relative;
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
		padding: clamp(7.5rem, 17vw, 11rem) 0 clamp(1rem, 3vw, 2rem);
	}

	.agent-map::before {
		content: '';
		position: absolute;
		inset: 10% 10% auto;
		height: min(46vw, 360px);
		border: 1px dashed rgb(245 130 32 / 0.34);
		border-radius: 999px;
		transform: rotate(-4deg);
		pointer-events: none;
	}

	.agent-orbit {
		position: absolute;
		top: 50%;
		left: 50%;
		z-index: 2;
		display: grid;
		width: clamp(9rem, 18vw, 13rem);
		aspect-ratio: 1;
		place-items: center;
		padding: 1rem;
		border-radius: 999px;
		background: #17120d;
		color: #fff;
		box-shadow: 0 26px 70px rgb(23 18 13 / 0.25);
		transform: translate(-50%, -50%);
		text-align: center;
	}

	.agent-orbit span {
		position: absolute;
		inset: -12px;
		border: 1px solid rgb(245 130 32 / 0.42);
		border-radius: inherit;
	}

	.agent-orbit strong {
		font-size: clamp(1.35rem, 2.4vw, 2.1rem);
		letter-spacing: -0.05em;
	}

	.agent-orbit small {
		color: rgb(255 255 255 / 0.68);
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: 0.12em;
	}

	.agent-card,
	.governance-grid article,
	.workflow-step {
		border: 1px solid rgb(36 24 10 / 0.08);
		border-radius: 24px;
		background: rgb(255 255 255 / 0.88);
		box-shadow: 0 16px 45px rgb(48 31 9 / 0.07);
	}

	.agent-card {
		min-height: 184px;
		padding: clamp(1rem, 2.2vw, 1.45rem);
	}

	.agent-card:nth-child(2),
	.agent-card:nth-child(4) {
		transform: translateY(-1rem);
	}

	.agent-card:nth-child(3),
	.agent-card:nth-child(5) {
		transform: translateY(1rem);
	}

	.agent-card span {
		color: #f58220;
		font-weight: 950;
		font-size: 0.75rem;
		text-transform: uppercase;
		letter-spacing: 0.14em;
	}

	.agent-card h3,
	.governance-grid h3 {
		font-size: clamp(1.12rem, 2vw, 1.45rem);
		letter-spacing: -0.03em;
		margin: 0.55rem 0;
	}

	.agent-card p,
	.governance-grid p {
		color: #685a50;
		line-height: 1.55;
		margin: 0;
	}

	.workflow-rail {
		display: grid;
		grid-template-columns: repeat(5, minmax(0, 1fr));
		gap: 0.72rem;
		margin-top: clamp(1.2rem, 4vw, 2.5rem);
	}

	.workflow-step {
		position: relative;
		min-height: 126px;
		padding: 1rem;
		overflow: hidden;
	}

	.workflow-step::after {
		content: '';
		position: absolute;
		inset: auto 1rem 1rem;
		height: 4px;
		border-radius: 999px;
		background: linear-gradient(90deg, #f58220, rgb(245 130 32 / 0.2));
		transform-origin: left;
	}

	.workflow-step span {
		display: block;
		color: rgb(143 78 16 / 0.55);
		font-weight: 950;
	}

	.workflow-step strong {
		display: block;
		margin-top: 1rem;
		font-size: clamp(1rem, 1.5vw, 1.25rem);
		line-height: 1.2;
		letter-spacing: -0.03em;
	}

	.governance-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 1rem;
	}

	.governance-grid article {
		padding: 1.1rem;
	}

	.closing-section {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1.5rem;
	}

	.closing-section p {
		max-width: 760px;
		font-weight: 760;
		color: #24180f;
	}

	@media (prefers-reduced-motion: no-preference) {
		.agent-orbit span {
			animation: sale-ai-pulse 3.2s ease-in-out infinite;
		}

		.workflow-step::after {
			animation: sale-ai-progress 3.6s ease-in-out infinite;
			animation-delay: calc(var(--step-index) * 0.18s);
		}

		.diagram-agent {
			animation: sale-ai-breathe 4s ease-in-out infinite;
		}
	}

	@keyframes sale-ai-pulse {
		0%, 100% { transform: scale(1); opacity: 0.72; }
		50% { transform: scale(1.08); opacity: 0.22; }
	}

	@keyframes sale-ai-progress {
		0%, 100% { transform: scaleX(0.28); opacity: 0.45; }
		50% { transform: scaleX(1); opacity: 1; }
	}

	@keyframes sale-ai-breathe {
		0%, 100% { transform: scale(1); }
		50% { transform: scale(1.025); }
	}

	@media (max-width: 980px) {
		.hero-section,
		.stats-section,
		.feature-section {
			grid-template-columns: 1fr;
		}

		.hero-section {
			min-height: auto;
		}

		.hero-visual {
			transform: none;
		}

		.layer-grid,
		.proof-grid,
		.governance-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.workflow-rail {
			grid-template-columns: repeat(3, minmax(0, 1fr));
		}
	}

	@media (max-width: 640px) {
		.page-wrap {
			width: min(100% - 1rem, 1180px);
		}

		h1 {
			font-size: clamp(2.2rem, 10.5vw, 3.25rem);
			max-width: 12.2ch;
		}

		.stats-grid,
		.layer-grid,
		.proof-grid,
		.agent-map,
		.workflow-rail,
		.governance-grid,
		.sales-diagram {
			grid-template-columns: 1fr;
		}

		.stats-grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.agent-map {
			padding: 0;
		}

		.agent-map::before,
		.agent-orbit {
			display: none;
		}

		.agent-card:nth-child(n) {
			transform: none;
		}

		.sales-diagram {
			min-height: auto;
			gap: 0.7rem;
			padding: 0.85rem;
		}

		.diagram-output {
			grid-column: auto;
			justify-self: stretch;
			width: 100%;
		}

		.diagram-phone {
			min-height: 168px;
		}

		.diagram-phone,
		.diagram-agent,
		.diagram-output {
			padding: 0.9rem;
		}

		.diagram-phone strong,
		.diagram-agent strong,
		.diagram-output strong {
			font-size: 1.12rem;
		}

		.closing-section {
			align-items: stretch;
			flex-direction: column;
		}

		.primary-link {
			width: 100%;
		}
	}
</style>
